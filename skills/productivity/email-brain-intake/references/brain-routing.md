# Brain artifact routing

Normal durable email updates are written only as concise provenance-bearing captures under `00 Inbox/Email Captures`. Canonical notes are not changed by this first implementation. Raw email bodies and quoted chains are never retained.
